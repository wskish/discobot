# Discobot Server - No Auth Mode (Default)
# Copy this file to .env and adjust as needed
#
# Usage: cp .env.example .env
#
# XDG Base Directory Compliance:
# By default, the server stores data in XDG-compliant directories:
#   - Database:     $XDG_DATA_HOME/discobot/discobot.db  (~/.local/share/discobot/)
#   - Workspaces:   $XDG_DATA_HOME/discobot/workspaces/  (~/.local/share/discobot/)
#   - SSH host key: $XDG_STATE_HOME/discobot/ssh_host_key (~/.local/state/discobot/)
#   - VZ data:      $XDG_STATE_HOME/discobot/vz/         (~/.local/state/discobot/)
#
# Override these with environment variables if needed.

# Server
PORT=3001

# Database (SQLite for local development)
# Default: sqlite3://$XDG_DATA_HOME/discobot/discobot.db
# DATABASE_DSN=sqlite3://./discobot.db

# Authentication disabled - uses anonymous user
AUTH_ENABLED=false

# CORS (add your frontend URL if different)
# Supports wildcards: http://*.localhost:3000
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://*.localhost:3000,http://*.localhost:3001

# Workspace directory
# Default: $XDG_DATA_HOME/discobot/workspaces
# WORKSPACE_DIR=./workspaces

# SSH Server
# Default host key path: $XDG_STATE_HOME/discobot/ssh_host_key
# SSH_HOST_KEY_PATH=./ssh_host_key
# SSH_PORT=3333
# SSH_ENABLED=true

# VZ (macOS Virtualization.framework)
# Default data dir: $XDG_STATE_HOME/discobot/vz
# VZ_DATA_DIR=./vz
# VZ_KERNEL_PATH=./vmlinuz
# VZ_INITRD_PATH=  # Not needed - SquashFS and virtio built into kernel
# VZ_BASE_DISK_PATH=./discobot-rootfs.squashfs

# Sandbox Providers
# Enable local provider (runs agent directly in workspace without containers)
# Default: false (only Docker provider is enabled)
# LOCAL_PROVIDER_ENABLED=true
# LOCAL_AGENT_BINARY=../agent-api/dev-agent

# Enable suggesting completions for files on the local directory
SUGGESTIONS_ENABLED=true